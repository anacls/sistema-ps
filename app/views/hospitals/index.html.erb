<p id="notice"><%= notice %></p>

<h1>Hospitais</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereço</th>
      <th>CNPJ</th>
      <th>Telefone</th>
      <th>Email</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @hospitals.each do |hospital| %>
      <tr>
        <td><%= hospital.nome %></td>
        <td><%= hospital.endereco %>, <%= hospital.numero %> - <%= hospital.bairro %> | <%= hospital.cidade %> - <%= hospital.estado %></td>
        <td><%= hospital.cnpj %></td>
        <td><%= hospital.telefone %></td>
        <td><%= hospital.email %></td>
        <td><%= link_to 'Show', hospital %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
